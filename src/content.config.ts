import { defineCollection, z } from 'astro:content';

// No content collections currently defined.
// Weekly programs are fetched live from the Google Calendar API via /api/events.

export const collections = {};
